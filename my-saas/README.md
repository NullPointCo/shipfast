# Django + Stripe Template

This is a starter template for a SaaS product using Django and Stripe.

To create a new project, run:

```
shipfast create my-saas --stack django-stripe
```

The template includes:

- Django web app with Stripe payment integration
- Basic authentication flow
- Example page displaying subscription status