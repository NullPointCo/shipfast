# Server-side Stripe keys (never exposed to the browser)
PRIVATE_STRIPE_SECRET_KEY=sk_test_xxx
PRIVATE_STRIPE_WEBHOOK_SECRET=whsec_xxx

# Public Price IDs created in the Stripe Dashboard (Products > Prices)
PUBLIC_STRIPE_STARTER_PRICE_ID=price_xxx
PUBLIC_STRIPE_PRO_PRICE_ID=price_xxx

# Public base URL (used for Checkout redirect URLs)
PUBLIC_BASE_URL=http://localhost:3000
