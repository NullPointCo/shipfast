# shipfast

Production-ready SaaS boilerplate generator. Create a full-stack starter in seconds.

## Install
```bash
git clone https://github.com/NullPointCo/shipfast
python3 shipfast.py --help
```

## Usage
```bash
# List available stacks
python3 shipfast.py list

# Create a project
python3 shipfast.py create my-saas --stack fastapi-stripe
python3 shipfast.py create my-webapp --stack nextjs-supabase
python3 shipfast.py create my-api --stack fastapi-supabase
python3 shipfast.py create my-webapp --stack sveltekit-supabase
python3 shipfast.py create my-app --stack sveltekit-stripe
python3 shipfast.py create my-flask --stack flask-stripe
python3 shipfast.py create my-django --stack django-stripe
python3 shipfast.py create my-django --stack django-supabase
python3 shipfast.py create my-express --stack express-stripe
python3 shipfast.py create my-saas --stack nextjs-lemonsqueezy
python3 shipfast.py create my-astro --stack astro-stripe
```

## Stacks

| Stack | Stack | Includes |
|-------|-------|----------|
| `fastapi-stripe` | FastAPI + PostgreSQL + Stripe | JWT auth, SQLAlchemy models, Docker Compose, Render deploy, tests |
| `flask-stripe` | Flask 3 + PostgreSQL + Stripe | JWT auth, Flask-SQLAlchemy models, Checkout subscriptions, webhooks, Docker, Render, tests |
| `django-stripe` | Django 5 + PostgreSQL + Stripe | Custom User, auth flows, Checkout subscriptions, webhooks, Docker, Render, tests |
| `django-supabase` | Django 5 + Supabase + Stripe | Supabase JWT auth middleware, Profile↔sub link, Checkout subscriptions, webhooks, Docker, Render, tests |
| `fastapi-supabase` | FastAPI + Supabase + Stripe | Supabase Auth/JWT, lazy clients, Checkout subscriptions, webhook, Docker, Render, tests |
| `nextjs-stripe` | Next.js 14 + Stripe | App Router, TypeScript, Checkout subscriptions, signed webhooks, Docker, pricing page |
| `nextjs-supabase` | Next.js 14 + Supabase | App Router, SSR auth, protected routes, TypeScript |
| `nextjs-lemonsqueezy` | Next.js 14 + Lemon Squeezy | App Router, TypeScript, hosted checkout, HMAC-signed webhooks, global tax handled, Docker |
| `astro-stripe` | Astro 4 + Stripe | Server-rendered, subscription Checkout, signed webhooks, pricing page, Docker |
| `express-stripe` | Node + Express + PostgreSQL + Stripe | JWT auth, bcrypt, Checkout subscriptions, webhook, Docker, Render, Jest tests |
| `sveltekit-supabase` | SvelteKit + Supabase | File-based routing, SSR auth, protected routes, TypeScript, `adapter-node` Docker |
| `sveltekit-stripe` | SvelteKit 2 + Stripe | SSR file routing, Stripe Checkout (subscriptions), signed webhooks, TypeScript, `adapter-node` Docker |

All 11 stacks ship with auth, a database layer, payment integration (Stripe /
Lemon Squeezy), Docker for local dev, a deploy blueprint (Render), and tests
where applicable.

## What you get
- Working backend with auth
- Database models
- Payment integration (Stripe / Lemon Squeezy)
- Docker + Compose for local dev
- Deploy config (Render)
- Tests

## Roadmap
- Remix + Planetscale
- More payment providers (Paddle, Polar)

Generated by NullPointerCo — *Earn. Build. Compute.*

## Support
If shipfast saves you time, please ⭐ the repo and consider [GitHub Sponsors](https://github.com/sponsors/NullPointCo). Stars = visibility = more templates.

*NullPointerCo — Earn. Build. Compute.*
