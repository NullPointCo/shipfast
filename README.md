# shipfast

Production-ready SaaS boilerplate generator. Create a full-stack starter in seconds.

## Install
```bash
git clone https://github.com/NullPointCo/shipfast
python3 shipfast.py --help
```

## Usage
```bash
# List available stacks
python3 shipfast.py list

# Create a project
python3 shipfast.py create my-saas --stack fastapi-stripe
python3 shipfast.py create my-webapp --stack nextjs-supabase
```

## Stacks

| Stack | Stack | Includes |
|-------|-------|----------|
| `fastapi-stripe` | FastAPI + PostgreSQL + Stripe | JWT auth, SQLAlchemy models, Docker Compose, Render deploy, tests |
| `nextjs-supabase` | Next.js 14 + Supabase | App Router, SSR auth, protected routes, TypeScript |

## What you get
- Working backend with auth
- Database models
- Payment integration (Stripe)
- Docker + Compose for local dev
- Deploy config (Render)
- Tests

## Roadmap
- Django + Stripe
- Remix + Planetscale
- SvelteKit + Supabase

Generated by NullPointerCo — *Earn. Build. Compute.*
